# Elin Mod 開発方針

このリポジトリには、主に BepInEx と Harmony を使用した Elin 向け C# Mod が含まれる。
個人用 AGENTS.md の一般方針に加え、このリポジトリでは以下を適用する。

## 基本方針

- 要求された機能で明示的に変更する場合を除き、ゲーム本体の挙動を維持する。
- ゲーム本体への変更範囲は可能な限り小さくする。
- ゲーム本体の実装を大量にコピーした Patch は、保守性とアップデート追従性の観点から避ける。
- Patch 対象を決める際は、可能なら呼び出し元・継承関係も MCP で確認する。
- Elin 内部の private/internal な実装や IL 配置は、ゲームアップデートで変わりうる implementation detail として扱う。
- ゲーム本体の挙動を説明する場合、可能であれば対象実装と呼び出し箇所を確認する。
- 実装を確認していないゲーム仕様を、名前や一般的なゲーム設計だけから断定しない。
- Elin は特殊処理がハードコードされている場合があるため、一般化された仕組みだけを見て「必ずこう動く」と判断しない。

## Harmony Patch

- Prefix / Postfix でゲーム本体処理を大量に複製せず自然に実装できる場合は、まずそれらを検討する。
- 変更対象がゲーム本体の制御フロー内部にあり、Prefix / Postfix では本体処理のコピーが必要になる場合は、Transpiler を正当な選択肢として扱う。
- 「Transpiler は壊れやすい」という理由だけで、不自然な Prefix / Postfix へ置き換えない。
- Transpiler は必要な instruction の周辺だけを狙い、変更範囲を限定する。
- matcher を作る前に、元の IL と周辺の制御フローを確認する。
- stack balance、branch target、label、exception block など、IL 書き換えで壊しやすい要素を確認する。
- ゲームアップデートで想定 IL が変わったとき、誤った箇所へ静かに Patch する実装より、検出可能な形で失敗する実装を優先する。
- matcher をアップデート耐性の名目で過度に緩くしない。
- このリポジトリが参照している Harmony のバージョンで利用できない API を前提にしない。特に `PatchCategory` が利用できるとは仮定しない。

## パフォーマンス

- 毎フレーム実行される UI、HoverGuide、HealthBar、戦闘中に高頻度で呼ばれる Patch は hot path として扱う。
- hot path では不要な allocation、Reflection、LINQ、Regex、文字列再構築、delegate 作成を避ける。
- Reflection を繰り返す必要がある場合は MethodInfo や delegate のキャッシュを検討する。
- 一見高コストに見えるという理由だけで設計を変更しない。呼び出し頻度と実測値を優先する。
- profiling 用コードや測定条件が残されている場合、比較が終わる前に理由なく削除しない。
- 既に測定済みの処理を変更する場合は、`docs/performance-notes.md` を確認する。

## ゲーム内部処理の調査

- Elin 本体の実装を調査する場合は、まず Elin MCP を使って型・メソッド・呼び出し関係を特定する。
- MCP だけでは情報が不足する場合、広範囲検索・差分確認・IL 確認が必要な場合は、ローカルの逆コンパイルソースを併用する。
- state の lifecycle を調べる場合は、生成、設定、参照、無効化、再構築、破棄まで追う。
- キャッシュされた一覧や計算結果については、設定変更後に自動再構築されるとは仮定しない。
- 単純な HP、数値、flag だけで semantic state を代用する前に、特殊なゲームシステムが存在しないか確認する。
- 一見 null 安全でないコードや不整合な実装があっても、実際の呼び出し順序・初期化保証を確認する前にゲーム本体のバグと断定しない。
- Mod 間互換性に影響する変更では、外部から観測される API / state と Patch 対象の両方を考える。

## 設定・保存データ

- save 固有設定と global 設定を混同しない。
- 設定ファイル形式を変更する場合は、既存ユーザーの migration と serializer の挙動を確認する。
- serializer に公開するためだけの member と、Mod 内 API として公開する member を混同しない。
- migration 用コードは永続的に残す前提にせず、互換対象期間と削除条件を意識する。

## UI・表示

- 情報追加では、表示できる情報量だけでなくレイアウト安定性と可読性も考慮する。
- 文字列生成を毎フレーム行う場合は、元 state が変化していないケースでの再計算を疑う。
- 表示更新を高速化するために、ゲーム状態の正しさを犠牲にしたキャッシュを導入しない。

## Localization

- ゲーム内の既存用語は、可能な限り Elin 公式の表記・Source Sheets に合わせる。
- 日本語、英語、簡体字中国語で UI 文言を作る場合、直訳よりゲーム UI として自然で短い表現を優先する。
- identifier、class 名、method 名、Harmony 用語は、説明文の中でも無理に日本語化しない。

## ビルド・翻訳確認

- Mod を build するときは、リポジトリルートの `tools/BuildMod.ps1 -Mod <Mod名>` を使用し、解決されたゲーム出力先と生成物を確認する。
- `-ValidationOutput` を使用した場合も、最終確認として通常出力への build を別途実行する。
- `package/LangMod` 配下の `SourceSheet.xlsx` を変更した場合は、`tools/TestSourceSheet.ps1 -Mod <Mod名>` を実行する。翻訳ファイルがない Mod は `Skipped` を正常な結果として扱う。
- 翻訳を追加した場合は、`-RequiredSourceId` で追加した Source ID の存在も確認し、SourceSheet と各言語 JSON の担当範囲を確認する。
- 最終報告には、build 実行コマンド、実際の出力先、確認した生成物を記載する。

## 調査資料

必要な場合だけ以下を読む。すべてを毎回読み込む必要はない。

- `docs/codex-context.md`: リポジトリと作業方針の補足
- `docs/elin-internals.md`: Elin 内部処理を調べる際の既知の注意点
- `docs/performance-notes.md`: 実測済みの性能情報
- `docs/decision-log.md`: コードだけでは分からない設計判断

## 確認

重要な変更を行った後は、可能な範囲で以下を行う。

1. 関連する呼び出し箇所と状態変更箇所を確認する。
2. 対象プロジェクトを build する。
3. compiler error / warning を確認する。
4. 最終 diff に要求外の変更が混ざっていないか確認する。
5. 必要ならゲーム内で確認すべきテストケースを列挙する。
6. 実行できなかった確認は明示する。
